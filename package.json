{
  "name": "mixpanel",
  "description": "A simple server-side API for mixpanel",
  "keywords": [
    "mixpanel",
    "analytics",
    "api",
    "stats"
  ],
  "version": "0.19.0",
  "homepage": "https://github.com/mixpanel/mixpanel-node",
  "author": "Carl Sverre",
  "license": "MIT",
  "main": "lib/mixpanel-node",
  "directories": {
    "lib": "lib"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/mixpanel/mixpanel-node.git"
  },
  "engines": {
    "node": ">=10.0"
  },
  "scripts": {
    "test": "vitest",
    "format": "prettier --write '**/*.{js,ts}'",
    "check-format": "prettier --check '**/*.{js,ts}'",
    "lint": "oxlint '**/*.{js,ts}'"
  },
  "types": "./lib/mixpanel-node.d.ts",
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.8",
    "nock": "^14.0.10",
    "oxlint": "^1.16.0",
    "prettier": "^3.6.2",
    "proxyquire": "^2.1.3",
    "vitest": "^4.0.8"
  },
  "dependencies": {
    "https-proxy-agent": "7.0.6"
  }
}
